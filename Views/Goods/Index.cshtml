@model IEnumerable<DisasterAlleviation.Models.DonGoodsModel>

<style>
    body {
        background-color: purple;
        font-family: Arial, sans-serif;
    }

    .card {
        background-color: white;
        border-radius: 10px;
        box-shadow: 0 4px 6px rgba(0, 0, 0, 0.1);
        padding: 20px;
        margin: 10px;
    }

    a:hover {
        color: orange;
    }

    a {
        color: black;
        text-decoration: none;
    }

    table {
        width: 100%;
        border-collapse: collapse;
        margin-top: 20px;
    }

    th, td {
        text-align: left;
        padding: 8px;
    }

    th {
        background-color: orange;
        color: white;
    }

    tr:nth-child(even) {
        background-color: #f2f2f2;
    }
</style>

<div class="card">
    <a asp-action="Create">New Goods Donation</a>
</div>

<div class="card">
    <table>
        <thead>
            <tr>
                <th>
                    @Html.DisplayNameFor(model => model.Date)
                </th>
                <th>
                    Disaster Description
                </th>
                <th>
                    No Items
                </th>
                <th>
                    @Html.DisplayNameFor(model => model.Category)
                </th>
                <th>
                    @Html.DisplayNameFor(model => model.Description)
                </th>
                <th>
                    @Html.DisplayNameFor(model => model.Username)
                </th>
                <th></th>
            </tr>
        </thead>
        <tbody>
            @foreach (var item in Model)
            {
                <tr>
                    <td>
                        @Html.DisplayFor(modelItem => item.Date)
                    </td>
                    <td>
                        <a asp-action="Details" asp-route-id="@item.DisasterID">@Html.DisplayFor(modelItem => item.DisasterDescription)</a>
                    </td>
                    <td>
                        @Html.DisplayFor(modelItem => item.Noitems)
                    </td>
                    <td>
                        @Html.DisplayFor(modelItem => item.Category)
                    </td>
                    <td>
                        @Html.DisplayFor(modelItem => item.Description)
                    </td>
                    <td>
                        @Html.DisplayFor(modelItem => item.Username)
                    </td>
                    <td>
                        <!-- Add any additional actions or buttons here if needed -->
                    </td>
                </tr>
            }
        </tbody>
    </table>
</div>
